---
interface Props {
  cnames: string;
}
const { cnames } = Astro.props;
---

<div class={`flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400 ${cnames}`}>
  <span class="w-2 h-2 rounded-full bg-green-500"></span>
  Available for remote roles
</div>
